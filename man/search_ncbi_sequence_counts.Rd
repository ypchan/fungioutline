% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_ncbi_sequence_counts.R
\name{search_ncbi_sequence_counts}
\alias{search_ncbi_sequence_counts}
\title{Search NCBI sequence counts}
\usage{
search_ncbi_sequence_counts(
  taxon,
  outline = NULL,
  taxon_index = NULL,
  db = "nuccore",
  match_synonym = TRUE,
  exact = TRUE,
  extra_query = NULL,
  use_cache = TRUE,
  cache_dir = NULL,
  delay_sec = 0.34,
  api_key = Sys.getenv("NCBI_API_KEY", unset = ""),
  search_fun = NULL,
  verbose = FALSE
)
}
\arguments{
\item{taxon}{Character vector of taxon names.}

\item{outline}{Optional standardized outline used to build a taxon index.}

\item{taxon_index}{Optional taxon index from \code{\link[=build_taxon_index]{build_taxon_index()}}. If both
\code{outline} and \code{taxon_index} are \code{NULL}, the packaged
\code{\link{fungi_taxon_index}} data is used.}

\item{db}{NCBI Entrez database. Defaults to \code{"nuccore"}.}

\item{match_synonym}{Logical. If \code{TRUE}, resolve synonym taxon names.}

\item{exact}{Logical. If \code{TRUE}, quote the taxon name in the NCBI Organism
query.}

\item{extra_query}{Optional additional Entrez query clause combined with
\code{AND}.}

\item{use_cache}{Logical. If \code{TRUE}, read and write cached results.}

\item{cache_dir}{Optional cache directory.}

\item{delay_sec}{Delay in seconds between uncached NCBI requests.}

\item{api_key}{NCBI API key. Defaults to \code{Sys.getenv("NCBI_API_KEY")}.}

\item{search_fun}{Optional mock search function for tests.}

\item{verbose}{Logical. If \code{TRUE}, emit cli progress messages.}
}
\value{
A tibble with one count row per input taxon.
}
\description{
Count-only wrapper around \code{\link[=search_ncbi_sequences]{search_ncbi_sequences()}}. It uses \code{retmax = 0} so
that only record counts are requested and returned.
}
\examples{
\dontrun{
fungioutline::search_ncbi_sequence_counts("Ascomycota")
}
}
