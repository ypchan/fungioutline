% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardize_outline.R
\name{standardize_outline}
\alias{standardize_outline}
\title{Standardize a fungal outline table}
\usage{
standardize_outline(
  data,
  trim_values = TRUE,
  empty_to_na = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{data}{A data frame containing a fungal outline table.}

\item{trim_values}{Logical. If \code{TRUE}, trim and squish whitespace in
character columns.}

\item{empty_to_na}{Logical. If \code{TRUE}, convert empty strings in character
columns to \code{NA}.}

\item{verbose}{Logical. If \code{TRUE}, emit an informational message.}
}
\value{
A tibble with standardized column names.
}
\description{
Converts fungal outline column names to snake case, trims character values,
converts empty strings to \code{NA}, and returns a tibble. Required outline
columns are moved before extra columns when present. Future \code{species} and
\code{species_syn} columns are preserved if they are present and ignored if absent.
}
\examples{
outline <- tibble::tibble(Kingdom = "Fungi", Genus = " Fusarium ")
fungioutline::standardize_outline(outline)
}
