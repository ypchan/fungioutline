% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_ncbi_sequences.R
\name{search_ncbi_sequences}
\alias{search_ncbi_sequences}
\title{Search NCBI for sequence availability}
\usage{
search_ncbi_sequences(
  taxon,
  outline = NULL,
  taxon_index = NULL,
  db = "nuccore",
  match_synonym = TRUE,
  exact = TRUE,
  extra_query = NULL,
  retmax = 20,
  use_cache = TRUE,
  cache_dir = NULL,
  delay_sec = 0.34,
  api_key = Sys.getenv("NCBI_API_KEY", unset = ""),
  search_fun = NULL,
  verbose = FALSE
)
}
\arguments{
\item{taxon}{Character vector of taxon names.}

\item{outline}{Optional standardized outline used to build a taxon index when
\code{taxon_index} is not supplied.}

\item{taxon_index}{Optional taxon index from \code{\link[=build_taxon_index]{build_taxon_index()}}.}

\item{db}{NCBI Entrez database. Defaults to \code{"nuccore"}.}

\item{match_synonym}{Logical. If \code{TRUE}, resolve synonym taxon names.}

\item{exact}{Logical. If \code{TRUE}, quote the taxon name in the NCBI Organism
query.}

\item{extra_query}{Optional additional Entrez query clause combined with
\code{AND}.}

\item{retmax}{Maximum number of record IDs to return.}

\item{use_cache}{Logical. If \code{TRUE}, read and write cached results.}

\item{cache_dir}{Optional cache directory.}

\item{delay_sec}{Delay in seconds between uncached NCBI requests.}

\item{api_key}{NCBI API key. Defaults to \code{Sys.getenv("NCBI_API_KEY")}.}

\item{search_fun}{Optional mock search function for tests. It must accept
\code{db}, \code{query}, \code{retmax}, and \code{api_key} arguments and return a list or data
frame with \code{count} and optional \code{ids}.}

\item{verbose}{Logical. If \code{TRUE}, emit cli progress messages.}
}
\value{
A tibble with one row per input taxon and columns including query,
count, IDs, cache status, and errors.
}
\description{
Searches an NCBI Entrez database, \code{nuccore} by default, for one or more taxon
names. When a taxon index or outline is supplied, taxon names are first
resolved through \code{\link[=get_lineage]{get_lineage()}}. Results are cached by database, query, and
\code{retmax}.
}
\examples{
\dontrun{
fungioutline::search_ncbi_sequences("Ascomycota", db = "nuccore")
}
}
