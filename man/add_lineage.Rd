% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_lineage.R
\name{add_lineage}
\alias{add_lineage}
\title{Add lineage columns to a data frame}
\usage{
add_lineage(
  data,
  taxon_col,
  outline = NULL,
  taxon_index = NULL,
  match_synonym = TRUE,
  best_match = TRUE,
  keep_unmatched = TRUE,
  suffix = "_lineage"
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{taxon_col}{Column containing taxon names. Supports bare column names
such as \code{genus} and strings such as \code{"genus"}.}

\item{outline}{Optional standardized outline used to build a taxon index.}

\item{taxon_index}{Optional taxon index from \code{\link[=build_taxon_index]{build_taxon_index()}}. If both
\code{outline} and \code{taxon_index} are \code{NULL}, the packaged
\code{\link{fungi_taxon_index}} data is used.}

\item{match_synonym}{Logical. If \code{TRUE}, match synonyms when no accepted name
match exists.}

\item{best_match}{Logical. If \code{TRUE}, keep one best lineage per input row.}

\item{keep_unmatched}{Logical. If \code{TRUE}, keep rows with unmatched taxa.}

\item{suffix}{Suffix appended to lineage columns that would otherwise
overwrite existing columns in \code{data}.}
}
\value{
A tibble containing the original data and appended lineage columns.
}
\description{
Annotates a data frame by resolving taxon names from one column against a
fungioutline taxon index. Bare column names and single column-name strings
are both supported.
}
\examples{
outline <- tibble::tibble(kingdom = "Fungi", phylum = "Ascomycota", genus = "Fusarium")
idx <- fungioutline::build_taxon_index(outline)
tibble::tibble(genus = "Fusarium") |>
    fungioutline::add_lineage(taxon_col = genus, taxon_index = idx)

tibble::tibble(genus = "Fusarium") |>
    fungioutline::add_lineage(taxon_col = genus)
}
